# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.0] - 2026-09-25

首个公开版本。

### 新增

- 注册 `.epub` 自定义编辑器（`epubReader.reader`），双击即读，资源管理器右键可指定用本扩展打开。
- EPUB 2（NCX）与 EPUB 3（NAV）目录解析，多级目录树、目录筛选、自动定位当前章。
- 全书全文搜索：上下文片段、命中数、耗时，点击结果跳转并高亮命中处。
- 书签：记录章节、滚动位置与当前段落摘录，可跳转与删除。
- 阅读进度记忆：按书保存章节与滚动比例，重开自动续读；`EPUB Reader: 继续阅读…` 列出最近阅读。
- 完整排版渲染：EPUB 内 CSS、`@import`、`url()`、图片、SVG、字体全部解析改写为 webview 资源。
- 安全清洗：移除章节内的 `script` / `iframe` / `on*` / `javascript:`，并配 CSP 双重拦截。
- 阅读设置：字号、行高、行宽、正文配色（跟随主题 / 浅色 / 护眼 / 深色）。
- 跟随 VS Code 主题的完整界面：工具栏、目录抽屉、状态栏、进度条。
- 快捷键：`[` `]` `h` `l` `t` `/` `b` `+` `-` `0` `?`，输入法组合期间不误触发。
- 编码嗅探：BOM 与 XML 声明中的 GBK / GB18030 / Big5 / UTF-16 均可正确解码。
- 零运行时依赖：自实现只读 ZIP（含 Zip64）与宽松 XML/XHTML 解析器。

### 修复

- `hidden` 属性被作者样式的 `display` 覆盖，导致快捷键浮层与加载提示永久可见且无法关闭。
- `@import` 内联后的 CSS 会被二次改写 `url()`。
- `<head>` 中的样式表未被处理。
